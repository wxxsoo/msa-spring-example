spring:
  profiles: local # file system에서 Config file을 읽어 올 수 있도록 native profile을 활성화시켜야 합니다.
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/server-configs
