spring:
  profiles: native
  message: native env
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/toy-projects/msa-spring-example/local-server-configs
  management:
    endpoints:
      web:
        base-path: /actuator
        exposure:
          include: ['bus-refresh'] # actuator의 bus-refresh endpoint 노출시켜줍니다