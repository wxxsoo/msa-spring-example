spring:
  profiles: native
  message: native env
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/toy-projects/msa-spring-example/local-server-configs